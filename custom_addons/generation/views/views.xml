<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="generation.list">
      <field name="name">generation list</field>
      <field name="model">generation.generation</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="generation.action_window">
      <field name="name">generation window</field>
      <field name="res_model">generation.generation</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="generation.action_server">
      <field name="name">generation server</field>
      <field name="model_id" ref="model_generation_generation"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="generation" id="generation.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="generation.menu_1" parent="generation.menu_root"/>
    <menuitem name="Menu 2" id="generation.menu_2" parent="generation.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="generation.menu_1_list" parent="generation.menu_1"
              action="generation.action_window"/>
    <menuitem name="Server to list" id="generation" parent="generation.menu_2"
              action="generation.action_server"/>
    -->
  </data>
</odoo>